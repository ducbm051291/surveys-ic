//
//  Constants.swift
//

enum Constants {

    enum API {}
    enum Gradient {}
    enum GrantType: String {

        case password
        case refreshToken = "refresh_token"
    }

    enum Regex {}
    enum View {}
}
